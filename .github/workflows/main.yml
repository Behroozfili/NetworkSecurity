name: Network Security CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  # 1. Continuous Integration (CI): Test the code
  continuous-integration:
    name: Continuous Integration - Testing
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      

    - name: Run Tests
      run: |
        pytest tests/

  # 2. Continuous Delivery: Build and Push to ECR
  continuous-delivery:
    name: Continuous Delivery - Push to ECR
    needs: continuous-integration 
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, Tag, and Push Image to ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: network-security-app
        IMAGE_TAG: latest
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

  # 3. Continuous Deployment: Deploy to App Runner
  #continuous-deployment:
    #name: Continuous Deployment - App Runner
    #needs: continuous-delivery # فقط اگر پوش به ECR موفق باشد اجرا می‌شود
    #runs-on: ubuntu-latest
    #steps:
    #- name: Configure AWS Credentials
      #uses: aws-actions/configure-aws-credentials@v1
      #with:
        #aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        #aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        #aws-region: us-east-1

    #- name: Deploy to AWS App Runner
      #run: |
        #aws apprunner start-deployment --service-arn ${{ secrets.APP_RUNNER_SERVICE_ARN }}--region us-east-1